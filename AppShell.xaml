<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="project.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:project"
    xmlns:pages="clr-namespace:project.Pages"
    Title="مشروع المكتبة">

    <ShellContent
        Title="مكتبتي"
        ContentTemplate="{DataTemplate pages:BooksPage}"
        Route="BooksPage" />

    
    
    <TabBar>

    <ShellContent
        Title="Home"
        Icon="home.png"
        ContentTemplate="{DataTemplate pages:BooksPage}" />


    <ShellContent
        Title="Ajouter"
        Icon="add.png"
        ContentTemplate="{DataTemplate pages:AddBookPage}" />

</TabBar>

</Shell>